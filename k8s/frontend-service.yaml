apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: silentflow
  labels:
    app: silentflow
    component: frontend
spec:
  selector:
    app: silentflow
    component: frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
